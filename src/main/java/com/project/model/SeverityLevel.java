package com.project.model;

/**
 * Case severity levels affecting priority and SLA deadlines.
 * Order: LOW < MEDIUM < HIGH < CRITICAL
 */
public enum SeverityLevel {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
